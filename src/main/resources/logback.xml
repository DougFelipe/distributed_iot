<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/sistema-distribuido.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/sistema-distribuido.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{50}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Configuração específica para componentes do sistema -->
    <logger name="br.ufrn.dimap.components" level="INFO" />
    <logger name="br.ufrn.dimap.communication" level="INFO" />
    <logger name="br.ufrn.dimap.patterns" level="WARN" />
    
    <!-- Reduzir ruído do FaultToleranceManager health checks -->
    <logger name="br.ufrn.dimap.patterns.fault.FaultToleranceManager" level="WARN" />
    <logger name="br.ufrn.dimap.patterns.observer.HeartbeatMonitor" level="WARN" />
    <logger name="br.ufrn.dimap.patterns.replication.DataReplicationManager" level="INFO" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>